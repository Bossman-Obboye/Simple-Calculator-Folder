import 'package:flutter/material.dart';

class CalculatorButtonForGridView extends StatelessWidget {
  const CalculatorButtonForGridView({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container();
  }
}